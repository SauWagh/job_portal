services:
  - type: web
    name: my-django-chatbot
    env: python
    region: oregon
    plan: free

    buildCommand: |
      pip install -r requirements.txt
      python manage.py migrate
      python manage.py collectstatic --noinput

    startCommand: gunicorn job_portal.wsgi:application
